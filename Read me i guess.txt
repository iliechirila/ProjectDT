//git
git clone {project http clone link}

//project folder 

npm install vue
npm install -g vue-cli
npm install --save axios

//client
vue init webpack client	//to create the client
npm install
npm start //==npm run dev
	//"dev": "webpack-dev-server --inline --progress --config build/webpack.dev.conf.js",
        //"start": "node build/dev-server.js",
        //"build": "node build/build.js",

//server
npm start  // starts the server on app.js. To change the destination that's being called by lint, see package.json in server, under scripts, and change the server in the json

//possible questions
--save basically adds that feature permanently
localhost 8081 -> used for get&post (vezi postman si cum se foloseste in part 1, capitol register setup)
